# Architecture Decision Records (ADRs)

Each decision follows the form: context → decision → rationale → alternatives rejected → consequences.

These expand on the decision table in `docs/spec.md §4`, adding full ADR form and covering runtime discoveries (D7–D12) that the original plan did not anticipate.

---

## D1 — Next.js App Router + TypeScript

**Context:** The product is a multi-tenant SaaS dashboard with per-locale routes, a future Supabase backend, and a handoff requirement for clean onboarding of any engineer. The tech stack needs to handle SSR, file-system routing, and straightforward backend wiring.

**Decision:** Use Next.js 15+ (App Router) with TypeScript throughout.

**Rationale:** App Router is the idiomatic way to handle `[locale]` routing, nested layouts (app shell vs auth shell), and server components that will later fetch from Supabase. TypeScript provides the typed data model that makes the seed-to-Supabase handoff safe. The combination is standard in the target hiring market.

**Alternatives rejected:**
- Single/multi-file vanilla React: no routing, no SSR, i18n is bespoke.
- React + Vite: lacks native nested layouts and server routing; i18n is a bolt-on.

**Consequences:** The project structure (`app/[locale]/`, route groups, `loading.tsx` convention) is opinionated. Engineers unfamiliar with App Router need a brief orientation. `params` is a `Promise` in Next.js 16 (see D8) — must be awaited.

---

## D2 — Tailwind CSS + shadcn/ui, themed to the token table

**Context:** The design system is a strict black/white "ledger" aesthetic where state is shown by inversion, never color. All interactive primitives need accessible focus, keyboard nav, and ARIA without hand-rolling them.

**Decision:** Use Tailwind CSS v3 (see D7) for styling and shadcn/ui for interactive primitives (Button, Badge, Sheet, DropdownMenu, Table, ToggleGroup, Input, Skeleton). Theme both via CSS variables mapped in `tailwind.config.ts`.

**Rationale:** shadcn copies component source into the repo (`components/ui/`), so the design system is not fighting a pre-styled library — it starts from a neutral base and the `app/globals.css :root` token layer drives the entire visual output. Radix UI (under shadcn) provides accessible Sheet/Dropdown/Dialog without custom ARIA.

**Alternatives rejected:**
- CSS Modules + custom properties: requires hand-wiring all a11y primitives.
- Premium ShadcnBlocks: marketing-shaped components fight the ledger aesthetic; extra cost.

**Consequences:** `components/ui/` is generated by `shadcn` CLI and is treated as owned code (editable). Token changes propagate via CSS variables — one edit in `globals.css` updates the entire UI. The `cn()` utility (`lib/utils.ts`, clsx + tailwind-merge) is the standard for conditional classes.

---

## D3 — next-intl for `[locale]` routing

**Context:** The app must support English and Arabic from day one, with correct `<html dir>` and `<html lang>`, per-locale message catalogs, and a clean path like `/en/overview` vs `/ar/overview`.

**Decision:** Use `next-intl` with a `[locale]` dynamic segment at the top of the App Router tree.

**Rationale:** next-intl is designed for App Router. It handles locale detection, routing, `<html dir>` injection via the root layout, and provides `useTranslations()` for both server and client components. No message keys are hardcoded in component source — all copy is in `messages/en.json` and `messages/ar.json`.

**Alternatives rejected:**
- react-i18next: less native to App Router; requires more wiring for `<html dir>`.
- Hand-rolled dictionary: boilerplate-heavy; no Plurals/ICU support.

**Consequences:** Every app route is prefixed `/en/` or `/ar/`. The middleware (`proxy.ts`) handles the root `/` redirect. The `LocaleSwitcher` component swaps the prefix in the current URL path to flip locale.

---

## D4 — No state management library

**Context:** The app is a frontend dashboard over seed data. Filters are in URL params; locale is in the routing; tenant is a simple React context.

**Decision:** No Redux, Zustand, or similar. Three sources of state: URL search params (filters, open order), React context (active tenant), next-intl (locale/routing).

**Rationale:** The scope does not justify the complexity. URL params are shareable and refresh-safe. React context with `useMemo` is sufficient for the tenant switcher. Adding a state library would add indirection without benefit.

**Alternatives rejected:**
- Redux: overkill for this surface area.
- Zustand: lighter but still adds a dependency and mental model.

**Consequences:** When Supabase is wired, server queries will be scoped by the tenant from context and the date from the server clock. No cache invalidation logic to rewrite.

---

## D5 — English + Arabic now, RTL via logical CSS properties

**Context:** The user's global requirement is EN/AR bilingual from day one.

**Decision:** Support both locales from the first commit. Use CSS logical properties (`margin-inline`, `padding-inline-start`, `inset-inline-start`, `border-e`, `border-s`, `text-end`, `ms-*`, `ps-*`) throughout so the RTL flip is structural, not a patch.

**Rationale:** Retrofitting RTL to an LTR-only build requires touching every layout. Logical properties cost nothing extra upfront and make the RTL flip entirely free — no mirroring hacks, no duplicate rules.

**Alternatives rejected:**
- EN-only LTR, RTL later: would require a large rework pass.
- RTL-ready-but-EN-only: partial; doesn't meet the stated requirement.

**Consequences:** Physical properties (`left`, `right`, `ml-*`, `pr-*`) are banned. Exception: `SheetContent side="right"` in `OrderDrawer` uses a physical side because shadcn Sheet only supports physical values (see D12). The `Measure` component uses `insetInlineStart` for tick positions.

---

## D6 — Evidence-first documentation + living `/styleguide` route

**Context:** The handoff requirement: any designer or engineer must be able to onboard cold without asking anyone for context.

**Decision:** Maintain a set of six documentation files (`docs/design-system.md`, `docs/components.md`, `docs/screens.md`, `docs/data-model.md`, `docs/decisions.md`, `docs/traceability.md`) as first-class deliverables, and a living `/styleguide` route that renders every component in every state in both LTR and RTL.

**Rationale:** Code comments are not traceable. Screenshots rot when code changes. The `/styleguide` route is real running code — it reflects the actual implementation and cannot diverge.

**Alternatives rejected:**
- Code comments only: not traceable, not visible to designers.
- Screenshot-based Figma annotations: rot immediately when code changes.

**Consequences:** The standing rule: no component or screen ships without a row in `components.md`/`screens.md` and an entry in `traceability.md`. This is enforced by convention (no automated check yet).

---

## D7 — Tailwind CSS v3, not v4

**Context:** The `create-next-app` scaffold installed Tailwind CSS v4 by default. The implementation plan was written for Tailwind v3 conventions.

**Decision:** Downgrade to Tailwind CSS v3 (`^3.4.19`, confirmed in `package.json`).

**Rationale:** Tailwind v4 uses a new `@theme` CSS-layer paradigm (instead of `tailwind.config.ts` + `@tailwind` directives). Every task in the plan — the HSL-var token mapping, the `theme.extend.colors`, the `@layer utilities` custom classes (`surface-inverted`, `nums`) — was written for v3. Adopting v4 would require rewriting the entire theming approach with zero UX difference.

**Alternatives rejected:**
- Tailwind v4: would require significant plan rework for no visible benefit at this scope.

**Consequences:** `tailwind.config.ts` and `@tailwind base/components/utilities` directives are v3 style. If upgraded to v4 later, the config migration is mechanical but non-trivial. `postcss.config.*` includes `autoprefixer` as required by v3.

---

## D8 — Next.js 16 (scaffold produced 16, not 15)

**Context:** The plan specified Next.js 15. The scaffold (`npx create-next-app@latest`) installed Next.js 16.2.9.

**Decision:** Accept Next.js 16. Do not downgrade.

**Rationale:** Next.js 16 is forward-compatible with the plan. The two breaking differences are handled:
1. The middleware file is named `proxy.ts` at the project root (Next.js 16 renamed `middleware.ts` to `proxy.ts` at some point, or the scaffold generated it with this name — the file at `proxy.ts` works correctly).
2. `params` in page/layout components is now a `Promise` and must be awaited. The locale layouts do this: `const { locale } = await params`.

**Alternatives rejected:**
- Downgrade to Next.js 15: unnecessary churn; no feature difference.

**Consequences:** Any future page or layout that receives `params` must destructure it with `await`. The `proxy.ts` file name must be preserved — renaming it to `middleware.ts` may or may not work depending on Next.js 16 resolution order. Verify before renaming.

---

## D9 — next-intl v4 (4.13.0 for Next.js 16 compatibility)

**Context:** The plan specified next-intl v3. Next.js 16 requires a compatible next-intl version.

**Decision:** Use next-intl v4 (`^4.13.0`, confirmed in `package.json`).

**Rationale:** next-intl v4 is required for Next.js 16 compatibility. The v4 API is backward-compatible with v3 for the patterns used in this project (`useTranslations`, `useLocale`, `getMessages`, `setRequestLocale`, `NextIntlClientProvider`, `defineRouting`, `getRequestConfig`). No API changes were needed.

**Alternatives rejected:**
- next-intl v3: would not be compatible with Next.js 16.

**Consequences:** When reading next-intl documentation, v4 docs apply. Migration from v3 to v4 was transparent for this project.

---

## D10 — HSL token approximation (shadcn compatibility)

**Context:** shadcn/ui primitives consume tokens as `hsl(var(--x))`. The design source of truth uses HEX values (`#FFFFFF`, `#0A0A0A`, etc.).

**Decision:** Express tokens as grayscale HSL channels in `app/globals.css :root`. HEX values remain the design source of truth (documented in `docs/design-system.md`). The HSL channels are approximations that produce visually identical grays.

**Rationale:** shadcn requires the CSS variable to contain only the HSL channels (not the `hsl()` wrapper), so the variable can be composed as `hsl(var(--x))` in Tailwind config. There is no way to use raw HEX in this pattern without forking shadcn.

**Alternatives rejected:**
- Storing HEX in CSS vars and wrapping differently: would break shadcn's `hsl(var(--x))` pattern.

**Consequences:** A comment on each `app/globals.css :root` line names the HEX token (e.g., `/* Paper #FFFFFF */`). Any designer working in Figma or a design tool should use the HEX values. The HSL channels are a build artifact.

---

## D11 — No auth backend; Login/Onboarding are UI flows

**Context:** The spec explicitly states "No auth backend" for this phase. Login and Onboarding are UI-only surfaces.

**Decision:** The "Sign in" button on Login and the "Continue to dashboard" button on Onboarding link directly to `/{locale}/overview` using Next.js `<Link>`. No session, no JWT, no API call.

**Rationale:** Auth wiring is Phase 2. Building it now would couple the frontend to a backend that doesn't exist yet, slowing the UI work. The data model and component API are designed so that adding real auth (Supabase Auth + RLS) requires no UI changes — only the data source in `TenantProvider` changes.

**Alternatives rejected:**
- Mock auth with fake localStorage session: adds complexity without benefit.
- Real Supabase auth: Phase 2 scope.

**Consequences:** Anyone who opens `/en/login` can click "Sign in" and reach the dashboard without credentials. This is expected. Security review must precede any production deployment.

---

## D12 — Mobile nav physical side by locale (shadcn Sheet limitation)

**Context:** The `MobileNav` off-canvas sheet should open from the inline-start side (left in LTR, right in RTL). CSS logical properties handle most layout, but `SheetContent` from shadcn/ui (Radix Dialog) only supports physical sides: `"left" | "right" | "top" | "bottom"`.

**Decision:** In `MobileNav`, pick `side` from locale: `const side = locale === "ar" ? "right" : "left"`. This achieves inline-start behavior using a physical value.

**Rationale:** The `side` prop is a Radix Dialog position enum and is not CSS-logical. There is no `"inline-start"` value. The locale-based branch is a minimal, readable workaround that achieves the correct visual behavior in both LTR and RTL without patching Radix.

**Alternatives rejected:**
- Always `side="left"`: wrong in RTL — the sheet would slide from the wrong side.
- Forking the Sheet component to add logical side support: high maintenance cost for a minor UI detail.

**Consequences:** If a third locale is added that is neither `en` nor `ar`, the RTL check must be updated. The condition is `locale === "ar"` — it is not generic RTL detection. A future improvement would be `locale === "ar" ? "right" : "left"` based on the `dir` attribute rather than a hardcoded locale string.

Note: `OrderDrawer` uses `side="right"` unconditionally. In an RTL layout, the drawer correctly appears from the left — but this is visual-right, which is the inline-end side in RTL. This is a known gap: the order drawer should use `side="left"` under RTL to open from inline-start. Deferred to Phase 2.

---

## D13 — Class-based dark theme with monochrome token flip

**Context:** An ops lead reviewing variance rows and failed orders late at night in a dim back-office environment needs the dashboard to be readable without glare. Problem blocks (`.surface-inverted` — the solid black rows that signal failed/returned orders or stock gaps) must remain visually distinct and legible even in a dark UI. The existing all-monochrome token layer is structurally dark-theme-ready: there are no hues to invert.

**Decision:** Add a `.dark` token block in `app/globals.css` that flips the grayscale palette — near-black (`7%`) for `--background`, near-white (`96%`) for `--foreground` — and wire it via `next-themes` with `attribute="class"`, `defaultTheme="system"`, `enableSystem`. `tailwind.config.ts` sets `darkMode: ["class"]`. The user toggle is `ThemeToggle` in `TopBar` and `MobileNav`. `.surface-inverted` is left structurally unchanged: in dark mode it still resolves to `hsl(var(--foreground))` background (near-white), so a problem block becomes a light-on-dark cell that still pops against the dark page.

**Rationale:**
- Default `system` means zero configuration cost for users whose OS is already in dark mode.
- A pure monochrome flip respects the "no color" rule (D2): no hues are introduced in dark mode.
- Near-black/near-white (not pure `#000`/`#FFF`) avoids harsh maximum-contrast edges while keeping WCAG AA compliance.
- Token-layer approach means no `dark:` utility sprawl: one CSS block in `globals.css` covers the entire UI.
- `next-themes` class strategy is compatible with Next.js App Router SSR without flash, using `disableTransitionOnChange` to suppress flicker on theme switch.

**Alternatives rejected:**
- `prefers-color-scheme` media query only: no user override; operator cannot lock to light if they prefer it despite OS setting.
- Per-component `dark:` Tailwind variants: would require touching every component and create a maintenance burden inconsistent with the token-layer principle.
- Dark-by-default: the design spec and existing seed data screenshots are light-first; dark is an overlay, not the base.

**Consequences:** A `ThemeProvider` wrapper (`components/theme/ThemeProvider.tsx`) must be present in the root layout above any component that reads `useTheme`. `ThemeToggle` (`components/shell/ThemeToggle.tsx`) uses `useSyncExternalStore` as a hydration guard to avoid the server/client mismatch that `next-themes` causes on first render. If a future color token is added, a corresponding `.dark` value must be added in the same commit.

---

## D14 — Inversion-aware `.interactive` hover utility

**Context:** The previous convention for clickable table rows was `className="cursor-pointer hover:bg-accent"`. This works for normal rows. However, a row that is simultaneously `surface-inverted` (e.g., a failed-order row or a stock-gap row) already has `background-color: hsl(var(--foreground))` — the near-black/near-white inverted surface. Applying `hover:bg-accent` on such a row in light mode produces a light-wash hover on a black background, which collapses to illegible contrast. In dark mode the problem is the same in reverse: the accent wash (dark) is nearly identical to the inverted surface (near-white becomes near-white-ish) and the hover is invisible.

**Decision:** Define a `.interactive` utility class in `app/globals.css @layer utilities` with three rules:

1. Normal hover: `background-color: hsl(var(--accent))` — the standard wash (works in light and dark via the token layer).
2. Inverted hover (`.surface-inverted.interactive:hover` and `.surface-inverted .interactive:hover`): `color-mix(in oklch, hsl(var(--foreground)) 86%, hsl(var(--background)))` — shifts the inverted surface 14% toward background, producing a perceptibly lighter (in light mode) or darker (in dark mode) hover without losing legibility.
3. Reduced-motion: `transition: none` when `prefers-reduced-motion: reduce`.

Replace all `cursor-pointer hover:bg-accent` instances on interactive rows with `className="interactive"`.

**Rationale:** A single utility handles all four combinations (normal/inverted × light/dark) without any `dark:` overrides or surface-specific branches in component code. `color-mix(in oklch …)` is supported in all modern browsers and produces perceptually uniform shifts.

**Alternatives rejected:**
- Keep `hover:bg-accent` and add `dark:hover:bg-…` per component: sprawl, error-prone, hard to audit.
- Remove hover from inverted rows: makes clickable rows indistinguishable from non-clickable ones when inverted.
- Use opacity trick (`hover:opacity-90`): opacity on a solid inverted block changes the text opacity too, reducing legibility.

**Consequences:** All LedgerTable clickable rows (and any other clickable surfaces) must use `className="interactive"` (not `cursor-pointer hover:bg-accent`). The `.interactive` class is documented in `design-system.md §3a` and in the `LedgerTable` usage example in `components.md`.
